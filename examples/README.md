# Examples

Examples are deliberately curated, small, and understandable without private
media. Operational truth belongs under `workspace/productions/<slug>/`.

The repository's neutral content-graph fixture is contract proof, not a channel
or production example. Promote a real example only by human choice after its
artifacts and independent reviews are safe to share; there is no promotion
command or history ledger.
