# Design

Use the repo root `DESIGN.md` as the channel style source.

This project is a 16:9 product/demo promo starter. Use it for SaaS demos, agent walkthroughs, UI reveals, and long-form motion inserts.

## Edit Notes

- Replace demo copy with the actual product or agent workflow.
- Use `assets/brand-tokens.css` for colors before hardcoding new values.
- Keep screenshots and UI recordings crisp; blurry UI is worse than no UI.
