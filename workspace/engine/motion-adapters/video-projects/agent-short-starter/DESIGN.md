# Design

Use the repo root `workspace/channel/DESIGN.md` as the channel style source.

This project is a 16:9 kinetic motion reference. Treat it as a pattern library for title sequences and high-energy transitions.

## Edit Notes

- Replace generic HyperFrames copy with the actual agent topic.
- Pull useful timing and transitions into new projects rather than forcing every video into this exact look.
- Inspect frames before rendering final.

