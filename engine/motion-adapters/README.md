# Optional motion adapters

HyperFrames assets remain in the historical `video-projects/` directories for
backward-compatible local use. They are optional overlays or explanatory
assets, not the Agentic Content System engine or its source of truth. The
declarative ACS contracts, FFmpeg outputs, approval, and proof stay local to
the content workspace.
