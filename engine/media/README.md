# Media boundary

ACS uses FFmpeg and ffprobe as its deterministic media boundary. Inputs are
declared in the local content workspace and outputs are recorded with hashes;
this directory documents the boundary rather than adding a second media
runtime.
